class ProductManager {
    constructor()}
     this.products = []

     addProduct (title, description, price, image,  code, stock) {

      this.product.push({title, description, price, image, code, stock })
    }

    getProduct() {
        return this.products;
    }

